        <div class="about_single_part_area bg-gray">
          <div class="about_us_content item">
              <div class="classy-table">
                  <div class="classy-table-cell">
                      <div class="about_text">
                          <h3><%= artic.title %></h3>
                          <div><%= artic.summary %></div>
                          <% if artic.link? %>
                          <a href="<%= artic.link %>" target="_blank" class="btn btn-pill btn-primary m-top-15">Les mer</a>
                          <% end %>
                      </div>
                  </div>
              </div>
          </div>
          <div class="about_thumb item">
              <%= image_tag(artic.image, :class => "", :alt => "") if artic.image? %>
          </div>            
        </div>
